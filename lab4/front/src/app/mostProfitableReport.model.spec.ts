import { MostProfitableReport } from './mostProfitableReport.model';

describe('MostProfitableReport', () => {
  it('should create an instance', () => {
    expect(new MostProfitableReport()).toBeTruthy();
  });
});
